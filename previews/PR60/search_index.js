var documenterSearchIndex = {"docs":
[{"category":"section","location":"decisions/#Design-Decisions","page":"Design Decisions","text":"This page records why AlignedSpans works the way it does, for maintainers and for anyone reimplementing similar functionality elsewhere. It's organized by topic, with references to the pull requests and issues where each decision was made.","title":"Design Decisions"},{"category":"section","location":"decisions/#Endpoints-and-interval-representation","page":"Design Decisions","text":"We depend on Intervals.jl to represent span endpoints internally, rather than working only in terms of TimeSpans.jl (PR #2). Originally (PR #1), converting an AlignedSpan to a TimeSpan was done by adding one nanosecond to fake an exclusive endpoint, but this wasn't robust across TimeSpans.jl versions (v0.2 and v0.3 represent spans differently, and v0.3 wasn't compatible with Onda at the time). Intervals.jl lets us represent open and closed endpoints explicitly, so to_interval can convert any span-like input (a TimeSpan, an Interval, or anything else supporting start/stop) into one closed-closed Interval{Nanosecond} before any rounding happens.\n\nWe canonicalize every input span to a closed-closed interval before rounding, rather than branching on open vs. closed endpoints inside the rounding logic (PR #41). The earlier approach handled the closed-open case (as produced by TimeSpan) with an ad hoc check inside the rounding functions, which missed a case and produced an off-by-one index at some sample rates. Converting to closed-closed once, in to_interval, removed the need for that check, at the cost of adding and subtracting a nanosecond in a few places when converting endpoints.\n\nThis mismatch between AlignedSpan's indices (inclusive-inclusive) and TimeSpan's endpoints (inclusive-exclusive) is visible directly:\n\nusing AlignedSpans, TimeSpans, Dates\n\naligned = AlignedSpan(1, 2:3) # indices 2 and 3 are both included\n\nTimeSpan(aligned) # TimeSpans.jl spans exclude their right endpoint: [1s, 3s)","title":"Endpoints and interval representation"},{"category":"section","location":"decisions/#The-index-time-conversion-convention","page":"Design Decisions","text":"For TimeSpans.start/TimeSpans.stop, we use start(span) = time_from_index(sample_rate, first_index) and stop(span) = time_from_index(sample_rate, last_index + 1) (PR #9). We considered instead using the time of the last sample plus one nanosecond, but rejected it for two reasons: durations would come out one sample period short of what's expected (5 samples at 1Hz would have duration 4s + 1ns instead of 5s), and two adjacent AlignedSpans would not map to two adjacent TimeSpans (there'd be a gap of almost one sample period between them). Taking the stop as the time of the sample after the last one avoids both problems, and matches the inclusive-inclusive convention of Julia's integer indices to the inclusive-exclusive convention Onda/TimeSpans use.\n\nOne consequence of this choice: rounding a span down or inward, then converting the result back to a TimeSpan, can produce a span that looks like it grew relative to the input. This is expected; see the warning under Sample index -> Time for a worked example. A smaller illustration of the underlying convention:\n\nusing AlignedSpans, TimeSpans, Dates\n\naligned = AlignedSpan(1, 2:3) # samples 2 and 3, at 1Hz\n\nTimeSpan(aligned) # starts when sample 2 occurs, stops when sample 4 would occur\n\nduration(aligned) # 2 samples at 1Hz is a sensible 2 seconds, not `1s + 1ns`","title":"The index -> time conversion convention"},{"category":"section","location":"decisions/#Sample-rate-representation:-floating-point-vs.-rational","page":"Design Decisions","text":"AlignedSpan.sample_rate is stored as Union{Int64,Rational{Int64}}, not Float64 (PR #44). We found that time_from_index, which is used throughout AlignedSpans to convert a sample index into a time, was affected by floating point error at at least one common sample rate (180 Hz): the computed nanosecond time for a given index could come out one nanosecond later than it should, tripping an internal consistency check we already had in place (issue #40).\n\nTo fix the underlying floating-point issue, we tested candidate implementations of time_from_index against an exact reference computed with BigInt/Rational{BigInt} arithmetic, across a range of sample rates that don't divide evenly into nanoseconds (180, 4//3, 1//3, 1//30, 44_000, ...) (PR #41). Converting the sample rate to a Rational and doing the arithmetic in Int128/Rational matched the exact reference in every case we tried, but repeating that conversion inside every call to time_from_index/index_from_time was measurably slower. So instead (PR #44), we do the conversion once, when an AlignedSpan is constructed: an integer sample rate stays an Int64, a sample rate that's already a Rational stays as-is, and anything else (typically a Float64) is converted with rationalize.\n\nThis does change the type stored in sample_rate for callers who were previously getting a Float64 there. We decided the correctness and performance benefits were worth it: sample_rate was never documented to be a Float64, and we judged the chance of downstream code depending on that incidental detail to be small.\n\nThe conversion is visible on the constructed AlignedSpan:\n\nusing AlignedSpans\n\nAlignedSpan(180, 1:100).sample_rate # already an Int, kept as-is\n\nAlignedSpan(1 / 30, 1:100).sample_rate # a Float64 gets rationalize'd\n\nAlignedSpan(1 // 30, 1:100).sample_rate # an exact Rational is kept as-is, with no approximation","title":"Sample rate representation: floating point vs. rational"},{"category":"section","location":"decisions/#Rounding-modes:-why-four,-and-how-they're-named","page":"Design Decisions","text":"We provide four rounding modes because they answer genuinely different questions about which samples a span corresponds to: RoundSpanDown matches TimeSpans.index_from_time's existing rounding behavior; RoundInward gives exactly the samples that occur within the span; ConstantSamplesRoundingMode guarantees a fixed sample count for spans of the same duration; RoundFullyContainedSampleSpans treats each sample as covering a span of time rather than an instant. The first two can disagree on the same input:\n\nusing AlignedSpans, TimeSpans, Dates\n\nspan = TimeSpan(Millisecond(1500), Millisecond(2500))\n\nAlignedSpan(1, span, RoundSpanDown) # rounds both endpoints down: 1.5s -> 1s, 2.5s -> 2s\n\nAlignedSpan(1, span, RoundInward) # shrinks until both endpoints land on samples\n\nNaming RoundSpanDown took a few iterations (PR #9). We initially called it RoundEndsDown, with an EndpointRoundingMode type, but RoundDown is already exported by Base for rounding a single value, and reusing it as part of a name for rounding both ends of an interval was judged confusing. We tried RoundEndpointsDown, but \"endpoint\" was still ambiguous about which endpoint. We settled on SpanRoundingMode/RoundSpanDown, which reads as \"the mode that rounds the span down.\"\n\nRoundFullyContainedSampleSpans was added later (PR #38), after RoundInward produced a result that a user found surprising (issue #36): rounding a span with RoundInward at 1/30Hz could produce an AlignedSpan that, once converted back to a TimeSpan, extended well past the original input. RoundInward was doing exactly what it's documented to do — including every sample whose instant occurs within the span — but that's not the answer you want if you think of each sample as covering a span of time, as with a hypnogram. Rather than changing RoundInward's semantics, we added RoundFullyContainedSampleSpans as a separate mode for that case.","title":"Rounding modes: why four, and how they're named"},{"category":"section","location":"decisions/#Warnings-and-asserts-for-internal-invariants","page":"Design Decisions","text":"A few places in interop.jl check, after rounding, that the rounding did what it was supposed to do, and either raise an error or log a warning if not, depending on AlignedSpans.ASSERTS_ON[] (which defaults to false) (PR #37). We expect these checks to never fire; if they do, it means there's a bug in AlignedSpans, not in the caller's input. We default to a rate-limited warning rather than an error because AlignedSpans is used in production data pipelines, and we'd rather flag a likely bug loudly than stop a batch job outright. Setting ASSERTS_ON[] = true turns these into hard errors, which is useful in testing. These checks are exactly what caught the floating-point issue described above (issue #40).\n\nusing AlignedSpans\n\nAlignedSpans.ASSERTS_ON[] # defaults to false: violations warn, they don't throw\n\nAlignedSpans.ASSERTS_ON[] = true # useful in tests, to turn violations into hard failures\n\nAlignedSpans.ASSERTS_ON[] = false # restore the default","title":"Warnings and asserts for internal invariants"},{"category":"section","location":"decisions/#Decisions-we've-deferred","page":"Design Decisions","text":"We allow AlignedSpans with indices that don't correspond to any actual sample of a given signal, including negative indices (issue #15, PR #16). We initially copied a sample_time >= 0 check from TimeSpans.index_from_time, where it made sense, but an AlignedSpan isn't necessarily tied to a specific Samples object with a known start and duration — it's a sample-rate-aligned span of time, so restricting it to only ever describe samples that already exist doesn't hold up. (Indexing an out-of-range AlignedSpan into an actual Samples object still raises a BoundsError at that point.)\n\nusing AlignedSpans\n\nAlignedSpan(1, -5:10) # allowed: this span starts before index 1 of some signal\n\nconsecutive_subspans supports keep_last=false so its behavior can match consecutive_overlapping_subspans (PR #19), but we haven't extended keep_last=true semantics to the overlapping case (issue #20): with overlapping windows, it's not obvious what a shorter last window should mean (if there's 8-fold overlap, do the last 7 windows all get shorter, or just the very last one?). We're leaving this until someone has a concrete use case.\n\nusing AlignedSpans\n\nspan = AlignedSpan(1, 1:10)\n\ncollect(consecutive_subspans(span, 3)) # keep_last=true (default): last window is shorter\n\ncollect(consecutive_subspans(span, 3; keep_last=false)) # drops the short last window instead\n\ncollect(consecutive_overlapping_subspans(span, 3, 2)) # overlapping windows only ever come in the \"keep_last=false\" style\n\nWe've sketched, but not implemented, TimeSpans.translate for AlignedSpan (issue #12) and merge_aligned_spans (issue #22). The generic TimeSpans.translate fallback already works for AlignedSpans, but shifts the time values directly and can change the resulting sample count; an AlignedSpan-specific implementation would shift by a number of samples instead, preserving the count:\n\nusing AlignedSpans, TimeSpans, Dates\n\nspan = AlignedSpan(3, 1:10) # 10 samples at 3Hz\n\nn_samples(span)\n\nshifted = TimeSpans.translate(span, Second(1)) # the generic fallback shifts the *time* and returns a TimeSpan\n\nn_samples(AlignedSpan(span.sample_rate, shifted, RoundSpanDown)) # re-rounding isn't guaranteed to match n_samples(span)\n\nWe may add this in a future release of AlignedSpans.jl.","title":"Decisions we've deferred"},{"category":"section","location":"examples/#Examples","page":"Examples","text":"","title":"Examples"},{"category":"section","location":"examples/#EEG-and-other-instant-like-samples","page":"Examples","text":"Most sensor data (EEG voltage traces, and similar) is naturally modeled with each sample as an instant in time: a sample is a single measurement taken at a point in time, not a summary of some span of time. This is the default way AlignedSpans treats samples, so RoundInward and RoundSpanDown (see Choosing a rounding mode) are the rounding modes to reach for.\n\nLet's consider the following TimeSpan\n\nusing TimeSpans, AlignedSpans, Dates\n\nspan = TimeSpan(Millisecond(1500), Millisecond(3500))\n\nIf we have a 1 Hz signal, there are various ways we can index into it using this TimeSpan. One option is to round the endpoints down:\n\n\ndown_span = AlignedSpan(1, span, RoundSpanDown)\n\nn_samples(down_span)\n\nThe second sample of our signal occurs at time 1s (since we have a 1Hz signal that starts at 0s). When we round the starting endpoint down from 1.5s to the nearest sample, we find that sample. This can be seen as \"the last sample that occurred before time 1.5s\".\n\nPerhaps instead we would like to round the endpoints \"inward\" to only consider samples occurring with the time span:\n\nin_span = AlignedSpan(1, span, RoundInward)\nn_samples(in_span)","title":"EEG and other instant-like samples"},{"category":"section","location":"examples/#Hypnograms-and-other-span-like-samples","page":"Examples","text":"Some signals, like hypnograms, are more naturally thought of as a sequence of spans rather than a sequence of instants: each sample summarizes some region of time (e.g. a 30 second sleep-stage epoch) rather than being measured at a single instant. For these, RoundFullyContainedSampleSpans is usually a better fit than RoundInward.\n\nusing TimeSpans, AlignedSpans, Dates\n\nsample_rate = 1 // 30\n\ninput = TimeSpan(0, Second(30) + Nanosecond(1))\n\naligned = AlignedSpan(sample_rate, input, RoundFullyContainedSampleSpans)\n\nTimeSpan(aligned)\n\nCompare this to rounding the same input with RoundInward:\n\nAlignedSpan(sample_rate, input, RoundInward)\n\nRoundInward includes both the sample at 00:00 and the sample at 00:30, since both occur within input. RoundFullyContainedSampleSpans includes only the sample at 00:00, since input doesn't fully contain the sample-span from 00:30 to 01:00.","title":"Hypnograms and other span-like samples"},{"category":"section","location":"examples/#Getting-a-consistent-number-of-samples-across-spans","page":"Examples","text":"RoundInward and RoundSpanDown can give a different number of samples for two spans of the same duration, depending on how each span happens to line up with the sample rate. If instead you want every span of a given duration to have the same number of samples, for example when cutting a signal into fixed-size windows, use ConstantSamplesRoundingMode:\n\nusing TimeSpans, AlignedSpans, Dates\n\nsample_rate = 3\n\nspan1 = TimeSpan(Millisecond(100), Millisecond(1100))\nspan2 = TimeSpan(Millisecond(200), Millisecond(1200))\n\nn_samples(AlignedSpan(sample_rate, span1, RoundInward))\nn_samples(AlignedSpan(sample_rate, span2, RoundInward))\n\nBoth spans have the same duration, but rounding inward can give a different number of samples depending on alignment. Using ConstantSamplesRoundingMode instead:\n\nn_samples(AlignedSpan(sample_rate, span1, ConstantSamplesRoundingMode(RoundDown)))\nn_samples(AlignedSpan(sample_rate, span2, ConstantSamplesRoundingMode(RoundDown)))\n\nnow both spans have the same number of samples.","title":"Getting a consistent number of samples across spans"},{"category":"section","location":"examples/#Splitting-a-span-into-fixed-size-or-sliding-windows","page":"Examples","text":"Once you have an AlignedSpan, use consecutive_subspans to split it into consecutive, non-overlapping windows, or consecutive_overlapping_subspans for sliding windows with a fixed hop between them:\n\nusing AlignedSpans\n\nspan = AlignedSpan(1, 1:10) # 10 samples at 1Hz\n\ncollect(consecutive_subspans(span, 3)) # windows of 3 samples; the last one may be shorter\n\ncollect(consecutive_subspans(span, 3; keep_last=false)) # drop the short last window instead\n\ncollect(consecutive_overlapping_subspans(span, 3, 2)) # windows of 3 samples, hopping by 2\n\nUse keep_last=false if your downstream code requires every window to have exactly the same number of samples; use the default keep_last=true if you'd rather keep every sample, even if the last window ends up shorter. consecutive_overlapping_subspans only supports the keep_last=false style, since it's not obvious what a partial window should mean once windows overlap.","title":"Splitting a span into fixed-size or sliding windows"},{"category":"section","location":"examples/#Spans-that-start-before-or-run-past-your-recording","page":"Examples","text":"An AlignedSpan doesn't need to correspond to samples that actually exist in some particular recording: it's a sample-rate-aligned span of time, and can have negative indices or indices beyond the end of any specific signal. This includes rounding a TimeSpan whose start time is negative, i.e. before index 1 of whatever it's relative to:\n\nusing AlignedSpans, TimeSpans, Dates\n\nAlignedSpan(1238, TimeSpan(Nanosecond(-32), Nanosecond(2)), RoundSpanDown) # the input TimeSpan starts before time 0\n\nas well as constructing a span directly from out-of-range indices:\n\nAlignedSpan(1, -5:10) # starts 5 samples before index 1 of some signal\n\nIndexing a span like this into an actual Samples object raises a BoundsError once you try, but constructing the AlignedSpan itself doesn't require that a signal covering the whole span already exists.","title":"Spans that start before or run past your recording"},{"category":"section","location":"examples/#Shifting-a-span-in-time","page":"Examples","text":"To shift an AlignedSpan while keeping the same number of samples, shift its indices directly rather than converting through a TimeSpan:\n\nusing AlignedSpans, Dates\n\nspan = AlignedSpan(3, 1:10) # 10 samples at 3Hz\n\nn = n_samples(span.sample_rate, Second(1)) # number of samples in a 1s shift\n\nshifted = AlignedSpan(span.sample_rate, (span.first_index + n):(span.last_index + n))\n\nn_samples(shifted) == n_samples(span)\n\nTimeSpans.translate(span, Second(1)) also works, but it shifts the underlying time and then re-rounds, which isn't guaranteed to preserve the number of samples. This may be improved in subsequent releases of AlignedSpans.jl.","title":"Shifting a span in time"},{"category":"section","location":"examples/#Computing-sample-counts-from-compound-durations","page":"Examples","text":"n_samples accepts any Dates.Period or Dates.CompoundPeriod, so durations built up from mixed units work directly, without converting to a single unit first:\n\nusing AlignedSpans, Dates\n\nn_samples(1e9, Minute(1) + Nanosecond(1))","title":"Computing sample counts from compound durations"},{"category":"section","location":"examples/#Passing-an-exact-sample-rate","page":"Examples","text":"If you know your sample rate exactly, for example 1//30 for a hypnogram sampled every 30 seconds, you can pass that Rational directly to AlignedSpan. If you instead pass a Float64, such as 1/30, it is converted to the nearest Rational using rationalize:\n\nusing AlignedSpans\n\nAlignedSpan(1 // 30, 1:100).sample_rate\n\nAlignedSpan(1 / 30, 1:100).sample_rate\n\nSee Design Decisions for why sample rates are stored as an Int or Rational rather than a Float64.","title":"Passing an exact sample rate"},{"category":"section","location":"examples/#Indexing-an-Onda-Samples-object:-TimeSpans-vs.-AlignedSpans","page":"Examples","text":"Onda's Samples supports indexing directly with a TimeSpan: samples[:, span]. Internally, this rounds span's endpoints down to the nearest samples, matching TimeSpans.index_from_time. That's exactly what RoundSpanDown does too, so indexing with AlignedSpan(sample_rate, span, RoundSpanDown) gives identical results to indexing with span directly, but also gives you the AlignedSpan itself to work with. Indexing with RoundInward instead can give you different samples, since it answers a different question (\"which samples occur entirely within span\" rather than \"round span's endpoints down\"):\n\nusing Onda, TimeSpans, AlignedSpans, Dates\n\nsample_rate = 1 # 1 Hz -> slow to exaggerate the effect\nsamples = Samples(permutedims(0:10), SamplesInfoV2(; sensor_type=\"feature\", channels=[\"a\"], sample_unit=\"microvolt\", sample_resolution_in_unit=0.5, sample_offset_in_unit=0.0, sample_type=UInt16, sample_rate), false)\nspan = TimeSpan(Millisecond(1500), Millisecond(3500))\n\nsamples[:, span] # indexing directly with a TimeSpan\n\nsamples[:, AlignedSpan(sample_rate, span, RoundSpanDown)] # matches samples[:, span]\n\nsamples[:, AlignedSpan(sample_rate, span, RoundInward)] # does not match: a different set of samples\n\nIf you need to keep track of exactly which span of time your extracted samples correspond to (for example, to plot them against the correct time axis), construct the AlignedSpan with RoundSpanDown first, and index with that instead of the original TimeSpan:\n\naligned_span = AlignedSpan(sample_rate, span, RoundSpanDown)\n\nsamples[:, aligned_span] == samples[:, span]\n\nTimeSpan(aligned_span) # the actual span covered by these samples, not `span` itself\n\nspan starts at 1.5s, but the samples returned start at 1s: TimeSpan(aligned_span) reflects that, while span does not.","title":"Indexing an Onda Samples object: TimeSpans vs. AlignedSpans"},{"category":"section","location":"#AlignedSpans","page":"Introduction","text":"See API documentation for how to construct AlignedSpans, along with some utilities, and Examples for further worked examples.\n\nAlignedSpans, like Onda, primarily treats samples as instants in time (rather than spans), and cares about \"which samples have occurred by such and such point in time\" rather than \"what sample-span is ongoing at such and such point in time\". See RoundFullyContainedSampleSpans for an exception, an approach that treats samples as spans.","title":"AlignedSpans"},{"category":"section","location":"#Time-Sample-index","page":"Introduction","text":"Timespans can be rounded (or \"aligned\") to the individual sample values by using the constructor AlignedSpan, which takes a sample_rate, a span, and a description of how to round time endpoints to sample indices. This constructs an AlignedSpan which supports Onda indexing. Internally, an AlignedSpan store sample indices, not times, and any rounding happens when it is created instead of when indexing into samples.\n\nRounding options:\n\nSpanRoundingMode: consists of a RoundingMode for the start and stop of the span.\nThe alias RoundInward = SpanRoundingMode(RoundUp, RoundDown), for example, constructs the largest span such that all samples are entirely contained within span.\nThe alias RoundSpanDown = SpanRoundingMode(RoundDown, RoundDown) matches the rounding semantics of TimeSpans.index_from_time(sample_rate, span).\nConstantSamplesRoundingMode consists of a RoundingMode for the start alone. The stop is determined from the start plus a number of samples which is a function only of the sampling rate and the duration of the span.\nRoundFullyContainedSampleSpans This is a special rounding mode which differs from the other rounding modes by associating each sample with a span (from the instant the sample occurs until just before the next sample occurs), and rounding inwards to the \"sample spans\" that are fully contained in the input span.\n\nSee Choosing a rounding mode for guidance on which of these to use.\n\nwarning: Warning\nIf the input span is not sample-aligned, meaning the start and stop of the input span are not exact multiples of the sample rate, the results can be non-intuitive at first, since the rounding is relative to the samples themselves rather than to the input span's endpoints. See the warning in Choosing a rounding mode for a worked example.\n\nAlso provides a helper consecutive_subspans to partition an AlignedSpan into smaller consecutive AlignedSpans of equal size (except possibly the last one).","title":"Time → Sample index"},{"category":"section","location":"#Sample-index-Time","page":"Introduction","text":"AlignedSpan's support TimeSpans.start and TimeSpans.stop, so they can be used as time spans. The semantics of this are:\n\nFor any index included in an AlignedSpan, the time at which the corresponding sample occurred (inclusive) to the time at which the next sample occurred (exclusive) is associated to the time representation of the span.\n\nAs an example, if the sample rate is 1, and sample indices 2:3 are associated to a span, then the associated TimeSpan is TimeSpan(Second(1), Second(3)). That's because sample 2 occur at time Second(1), and is considered to \"last\" until sample 3, which occurs at Second(2). Next, sample 3 occurs at time Second(2) and is considered to \"last\" until sample 4, which occurs at Second(3). Therefore, the total span associated to 2:3 is Second(1) to Second(3).\n\nIn diagram form, the inclusive interval of sample indices [2, 3] is associated inclusive-exclusive interval of seconds, [1, 3):\n\nIndex       1   [2    3]    4     5\nTime (s)    0   [1    2     3)    4\n\nThis choice of conversion matches the inclusive-inclusive indexing of Julia integer indices to the inclusive-exclusive semantics Onda/TimeSpans use, and allows for roundtripping and sensible durations:\n\njulia> using AlignedSpans, TimeSpans, Dates\n\njulia> aligned = AlignedSpan(1, 2:3)\nAlignedSpan(1, 2, 3)\n\njulia> ts = TimeSpan(aligned)\nTimeSpan(00:00:01.000000000, 00:00:03.000000000)\n\njulia> aligned == AlignedSpan(1, ts, RoundInward)\ntrue\n\njulia> aligned == AlignedSpan(1, ts, RoundSpanDown)\ntrue\n\njulia> duration(aligned) == duration(ts) == Second(2)\ntrue\n\nwarning: Warning\nFor non-integer sample rates, roundtripping perfectly is not always possible. Sample rates that aren't already an Int or a Rational are converted with rationalize; see Design Decisions for why.\n\nwarning: Warning\nBecause the stop of the time representation of an AlignedSpan is the time at which the sample after the last included one occurs, rounding a span and then converting it back to a TimeSpan can give a result that looks larger than the input. For example, at 1/30Hz, the samples occur at 00:00, 00:30, 01:00, and so on. The span TimeSpan(0, Second(30) + Nanosecond(1)) contains two samples, the ones at 00:00 and 00:30:julia> using TimeSpans, AlignedSpans, Dates\n\njulia> sample_rate = 1 // 30\n1//30\n\njulia> input = TimeSpan(0, Second(30) + Nanosecond(1))\nTimeSpan(00:00:00.000000000, 00:00:30.000000001)\n\njulia> aligned = AlignedSpan(sample_rate, input, RoundInward) # or RoundSpanDown\nAlignedSpan(1//30, 1, 2)\n\njulia> TimeSpan(aligned)\nTimeSpan(00:00:00.000000000, 00:01:00.000000000)Even though RoundInward and RoundSpanDown both round the right endpoint down, TimeSpan(aligned) is TimeSpan(0, Second(60)), not TimeSpan(0, Second(30) + Nanosecond(1)). That's because indices 1 and 2 correspond to the time from the first sample until just before the sample that would come after index 2, which occurs at Second(60).","title":"Sample index → Time"},{"category":"section","location":"#Motivation","page":"Introduction","text":"Let's say I want to plot some samples over time, and I have a nice function plot(::TimeSpan, ::Samples) to use.\n\nusing TimeSpans, Onda, Dates\nsample_rate = 1 # 1 Hz → slow to exaggerate the effect\nsamples = Samples(permutedims(0:10), SamplesInfoV2(; sensor_type=\"feature\", channels=[\"a\"], sample_unit=\"microvolt\", sample_resolution_in_unit=0.5, sample_offset_in_unit=0.0, sample_type=UInt16, sample_rate), false)\nspan = TimeSpan(Millisecond(1500), Millisecond(4000))\n\nNow I want to execute some call\n\nplot(span, samples[:, span])\n\nfor some plot that understands TimeSpans – doesn't matter what function, exactly.\n\nWhat is wrong with this?\n\nLet's take a look at the samples we are plotting:\n\nsamples[:, span] # TimeSpans v0.2; v0.3 will have one more sample\n\nThese are three samples that correspond to times 1s, 2s, and 3s. However, what we gave to the x-axis of our plotting function is TimeSpan(Millisecond(1500), Millisecond(3500)), which starts at 1.5s and goes to 3.5s. In other words, our plot will have an incorrect 0.5s offset!\n\nNote that plot is just an example; any function where one is separately passing both a \"timespan of interest\" and \"feature values from that timespan\" will have similar issues if one isn't careful about what exactly samples[:, span] is doing.","title":"Motivation"},{"category":"section","location":"#The-fix","page":"Introduction","text":"Let's take the same setup, with our\n\nspan\n\nThis time, we do\n\nusing AlignedSpans\naligned_span = AlignedSpan(samples.info.sample_rate, span, RoundSpanDown)\nsamples[:, aligned_span]\n\nHere, I get the same samples. However, now I have the actual span corresponding to those samples, namely aligned_span. So if I call my\n\nplot(aligned_span, samples[:, aligned_span])\n\nI'll have the correct alignment between the points on the x-axis and y-axis.","title":"The fix"},{"category":"section","location":"API/#API-documentation","page":"API Documentation","text":"","title":"API documentation"},{"category":"section","location":"API/#Choosing-a-rounding-mode","page":"API Documentation","text":"Use RoundSpanDown by default, or whenever you need to match the rounding Onda/TimeSpans already use elsewhere (TimeSpans.index_from_time(sample_rate, span)).\nUse RoundInward when you need exactly the samples that occur within a span and no others, treating each sample as an instant in time.\nUse ConstantSamplesRoundingMode when you need spans of the same duration to always produce the same number of samples, regardless of how they line up with the sample rate (for example, when cutting a signal into fixed-size windows).\nUse RoundFullyContainedSampleSpans when each sample represents a span of time rather than an instant (for example, a hypnogram, where each sample is a sleep-stage label covering some number of seconds), and you want only the sample-spans that are fully contained in the input span.\n\nwarning: Warning\nRoundInward and RoundFullyContainedSampleSpans can give different results because they answer different questions about the same input span. Consider a 1/30Hz signal and the span TimeSpan(0, Second(30) + Nanosecond(1)), which contains the samples at 00:00 and 00:30:julia> using TimeSpans, AlignedSpans, Dates\n\njulia> sample_rate = 1 // 30\n1//30\n\njulia> input = TimeSpan(0, Second(30) + Nanosecond(1))\nTimeSpan(00:00:00.000000000, 00:00:30.000000001)\n\njulia> AlignedSpan(sample_rate, input, RoundInward)\nAlignedSpan(1//30, 1, 2)\n\njulia> AlignedSpan(sample_rate, input, RoundFullyContainedSampleSpans)\nAlignedSpan(1//30, 1, 1)RoundInward includes both samples, since both occur within input. RoundFullyContainedSampleSpans includes only the first one, since input doesn't fully contain the sample-span from 00:30 to 01:00. See Design Decisions for why there are four separate rounding modes rather than one mode that tries to handle every case.","title":"Choosing a rounding mode"},{"category":"section","location":"API/#Interface-for-conversion-from-time-spans","page":"API Documentation","text":"In order to support conversion of time span types to AlignedSpan's, three methods may be defined. These are not exported, because they are generally not used directly, but rather defined in order to facilitate use of the AlignedSpan constructors.","title":"Interface for conversion from time spans"},{"category":"type","location":"API/#AlignedSpans.AlignedSpan","page":"API Documentation","text":"AlignedSpan(sample_rate::Number, first_index::Int, last_index::Int)\n\nConstruct an AlignedSpan directly from a sample_rate and sample indices first_index and last_index.\n\n\n\n\n\n","title":"AlignedSpans.AlignedSpan"},{"category":"type","location":"API/#AlignedSpans.SpanRoundingMode","page":"API Documentation","text":"SpanRoundingMode(start::RoundingMode, stop::RoundingMode)\n\nCreates a rounding object for AlignedSpan to indicate how the AlignedSpan's endpoints should be determined from a given spans endpoints'.\n\n\n\n\n\n","title":"AlignedSpans.SpanRoundingMode"},{"category":"constant","location":"API/#AlignedSpans.RoundSpanDown","page":"API Documentation","text":"RoundSpanDown = SpanRoundingMode(RoundDown, RoundDown)\n\nThis is a rounding mode where both ends of the time interval are rounded downwards.\n\nExample\n\nConsider a signal with sample rate 1 Hz.\n\nIndex       1   2   3   4   5\nTime (s)    0   1   2   3   4\n\nNow, consider the time span 1.5s (inclusive) to 2.5s (exclusive). Using brackets to highlight this span:\n\nIndex       1   2     3     4   5\nTime (s)    0   1  [  2  )  3   4\n\nIn code, this span is described by\n\njulia> using AlignedSpans, Dates, TimeSpans\n\njulia> ts = TimeSpan(Millisecond(1500), Millisecond(2500))\nTimeSpan(00:00:01.500000000, 00:00:02.500000000)\n\nIf we round both ends of the interval down to the nearest sample, the start of the interval becomes 1s, and the stop of the interval becomes 2s. Thus, the associated samples are at indices 2:3. And indeed,\n\njulia> aligned = AlignedSpan(1, ts, RoundSpanDown)\nAlignedSpan(1, 2, 3)\n\njulia> AlignedSpans.indices(aligned)\n2:3\n\ngives an AlignedSpan with sample indices 2:3.\n\n\n\n\n\n","title":"AlignedSpans.RoundSpanDown"},{"category":"constant","location":"API/#AlignedSpans.RoundInward","page":"API Documentation","text":"RoundInward = SpanRoundingMode(RoundUp, RoundDown)\n\nThis is a rounding mode where both ends of the time interval are rounded \"inwards\" to construct the largest span of sample indices such that all samples, as instants of time, occur within the span.\n\nExample\n\nConsider a signal with sample rate 1 Hz.\n\nIndex       1   2   3   4   5\nTime (s)    0   1   2   3   4\n\nNow, consider the time span 1.5s (inclusive) to 2.5s (exclusive). Using brackets to highlight this span:\n\nIndex       1   2     3     4   5\nTime (s)    0   1  [  2  )  3   4\n\nIn code, this span is described by\n\njulia> using AlignedSpans, Dates, TimeSpans\n\njulia> ts = TimeSpan(Millisecond(1500), Millisecond(2500))\nTimeSpan(00:00:01.500000000, 00:00:02.500000000)\n\nThe only sample within the span is at index 3. And indeed,\n\njulia> aligned = AlignedSpan(1, ts, RoundInward)\nAlignedSpan(1, 3, 3)\n\njulia> AlignedSpans.indices(aligned)\n3:3\n\ngives an AlignedSpan with sample indices 3:3.\n\n\n\n\n\n","title":"AlignedSpans.RoundInward"},{"category":"constant","location":"API/#AlignedSpans.RoundFullyContainedSampleSpans","page":"API Documentation","text":"RoundFullyContainedSampleSpans\n\nThis is a special rounding mode which differs from the other rounding modes by associating each sample with a span (from the instant the sample occurs until just before the next sample occurs), and rounding inwards to the \"sample spans\" that are fully contained in the input span.\n\nThis is in some sense a stricter form of inward rounding than provided by RoundInward.\n\nExample\n\nConsider a signal with sample rate 1 Hz.\n\nIndex       1   2   3   4   5\nTime (s)    0   1   2   3   4\n\nNormally, we consider each sample to occur at an instant of time. For RoundFullyContainedSampleSpans, we consider the span between a sample occurring and the next sample. (This usually does not make sense for digital sensors, but can make sense for things like hypnograms in which a sample summarizes some region of time).\n\nThus, to each index (each sample), we associate a 1s closed-open span of time:\n\nIndex        1     2     3     4     5\nSpan (s)    [0   )[1   )[2   )[3   )[4    )\n\nThe span durations are 1/sample_rate, which is 1s in this example.\n\nNow, consider the input time span 1.5s (inclusive) to 3.5s (exclusive). Using brackets to highlight this span:\n\nIndex        1     2     3     4     5\nSpan (s)    [0   )[1   )[2   )[3   )[4    )\nTime (s)     0     1  [  2     3  )  4\n\nThis input span contains only one sample-span, namely [2s, 3s). The span from [1s, 2) is not fully contained, nor is the span from [3s, 4s).\n\nThus, in this scenario, RoundFullyContainedSampleSpans would give a single sample, that at index 3, associated to [2s, 3s).\n\nIn code, this span is described by\n\njulia> using AlignedSpans, Dates, TimeSpans\n\njulia> ts = TimeSpan(Millisecond(1500), Millisecond(3500))\nTimeSpan(00:00:01.500000000, 00:00:03.500000000)\n\njulia> aligned = AlignedSpan(1, ts, RoundFullyContainedSampleSpans)\nAlignedSpan(1, 3, 3)\n\njulia> AlignedSpans.indices(aligned)\n3:3\n\n\n\n\n\n","title":"AlignedSpans.RoundFullyContainedSampleSpans"},{"category":"method","location":"API/#AlignedSpans.AlignedSpan-Tuple{Any, Any, SpanRoundingMode}","page":"API Documentation","text":"AlignedSpan(sample_rate, span, mode::SpanRoundingMode)\n\nCreates an AlignedSpan by rounding the left endpoint according to mode.start, and the right endpoint by mode.stop.\n\nIf mode.start==RoundUp, then the left index of the resulting span is guaranteed to be inside span. This is accomplished by checking if the left endpoint of the span is exclusive, and if so, incrementing the index after rounding when necessary.\n\nLikewise, if mode.stop==RoundDown, then the right index of the resulting span is guaranteed to be inside span. This is accomplished by checking if the right endpoint of the span is exclusive, and if so, decrementing the index after rounding when necessary.\n\nNote: span may be of any type which which provides methods for AlignedSpans.start_index_from_time and AlignedSpans.stop_index_from_time.\n\nwarning: Warning\nIf the input span is not sample-aligned, meaning the start and stop of the input span are not exact multiples of the sample rate, the results can be non-intuitive at first. Each underlying sample is considered to occur at some instant in time (not, e.g. over a span of duration of 1/sample_rate), and the rounding is relative to the samples themselves.So for example:julia> using TimeSpans, AlignedSpans, Dates\n\njulia> sample_rate = 1/30\n0.03333333333333333\n\njulia> input = TimeSpan(0, Second(30) + Nanosecond(1))\nTimeSpan(00:00:00.000000000, 00:00:30.000000001)\n\njulia> aligned = AlignedSpan(sample_rate, input, RoundInward) # or RoundSpanDown\nAlignedSpan(1//30, 1, 2)\n\njulia> TimeSpan(aligned)\nTimeSpan(00:00:00.000000000, 00:01:00.000000000)\nEven though we have specified RoundInward or RoundSpanDown, both of which round the right-endpoint down, the resulting sample-aligned TimeSpan(aligned) is TimeSpan(0, Second(60)).How can this be? Clearly Second(60) > Second(30) + Nanosecond(1), so what is going on?To understand this, note that at 1/30Hz, the samples occur at 00:00, 00:30, 00:60, and so forth. The original input span includes two samples, the one at 00:00 and the one at 00:30. Rounding inward to include only samples that occur within the span means including both samples (see RoundFullyContainedSampleSpans for alternate behavior).When converting back to TimeSpans, AlignedSpans canonicalizes AlignedSpan(1/30, 1, 2) as TimeSpan(0, Second(60)), representing these two samples as the time from the first sample until just before the not-included sample-3 (which occurs at Second(60)), using that TimeSpan's exclude their right endpoint.AlignedSpans could make a different choice to e.g. canonicalize samples to only add an additional nanosecond, but that has its own issue (e.g. TimeSpan(AlignedSpan(sample_rate, 1, 2)) and TimeSpan(AlignedSpan(sample_rate, 3, 4)) would not be consecutive).\n\nSee also AlignedSpan(sample_rate, span, mode::RoundingModeFullyContainedSampleSpans).\n\n\n\n\n\n","title":"AlignedSpans.AlignedSpan"},{"category":"type","location":"API/#AlignedSpans.ConstantSamplesRoundingMode","page":"API Documentation","text":"ConstantSamplesRoundingMode(start::RoundingMode)\n\nCreates a rounding object for AlignedSpan to indicate the AlignedSpan should be constructed by the start and duration of the span, without regard to its stop.\n\nIf two spans have the same duration, then the resulting AlignedSpan's will have the same number of samples when constructed with this rounding mode.\n\nSee also AlignedSpan(sample_rate, span, mode::ConstantSamplesRoundingMode).\n\n\n\n\n\n","title":"AlignedSpans.ConstantSamplesRoundingMode"},{"category":"method","location":"API/#AlignedSpans.AlignedSpan-Tuple{Any, Any, ConstantSamplesRoundingMode}","page":"API Documentation","text":"AlignedSpan(sample_rate, span, mode::ConstantSamplesRoundingMode)\n\nCreates an AlignedSpan whose left endpoint is rounded according to mode.start, and whose right endpoint is determined so by the left endpoint and the number of samples, given by AlignedSpans.n_samples(sample_rate, duration(span)).\n\nInterface: span may be of any type which which provides a method for AlignedSpans.start_index_from_time and TimeSpans.duration.\n\nMore detailed information\n\nThis is designed so that if AlignedSpan(sample_rate, span, mode::ConstantSamplesRoundingMode) is applied to multiple spans, with the same sample_rate, and the same durations, then the resulting AlignedSpan's will have the same number of samples.\n\nFor this reason, we ask for TimeSpans.duration(span) to be defined, rather than a n_samples(span) function: the idea is that we want to only using the duration and the starting time, rather than the actual number of samples in this particular span.\n\nIn contrast, RoundInward provides an AlignedSpan which includes only (and exactly) the samples which occur within span (as instants in time), while AlignedSpan(sample_rate, span, ::RoundingModeFullyContainedSampleSpans) provides an AlignedSpan consisting of the full spans from each sample until the next sample occurs that are contained in the input span.\n\nIf one wants to create a collection of consecutive, non-overlapping, AlignedSpans each with the same number of samples, then use consecutive_subspans instead.\n\n\n\n\n\n","title":"AlignedSpans.AlignedSpan"},{"category":"function","location":"API/#AlignedSpans.consecutive_subspans","page":"API Documentation","text":"consecutive_subspans(span::AlignedSpan, duration::Period; keep_last=true)\nconsecutive_subspans(span::AlignedSpan, n_window_samples::Int; keep_last=true)\n\nCreates an iterator of AlignedSpan such that each AlignedSpan has consecutive sample indices which cover the original span's sample indices (when keep_last=true).\n\nIf keep_last=true (the default behavior), then the last span may have fewer samples than the others, and\n\nEach span has n_window_samples samples (which is calculated as n_samples(span.sample_rate, duration) if duration::Period is supplied), except possibly\n\nthe last one, which may have fewer.\n\nThe number of subspans is given by cld(n_samples(span), n_window_samples),\nThe number of samples in the last subspan is r = rem(n_samples(span), n_window_samples) unless r=0, in which\n\ncase the the last subspan has the same number of samples as the rest, namely n_window_samples.\n\nAll of the sample indices of span are guaranteed to be covered by exactly one subspan\n\nIf keep_last=false, then all spans will have the same number of samples:\n\nEach span has n_window_samples samples (which is calculated as n_samples(span.sample_rate, duration) if duration::Period is supplied)\nThe number of subspans is given by fld(n_samples(span), n_window_samples)\nThe last part of the span's sample indices may not be covered (when we can't fit in another subspan of length n_window_samples)\n\n\n\n\n\n","title":"AlignedSpans.consecutive_subspans"},{"category":"function","location":"API/#AlignedSpans.consecutive_overlapping_subspans","page":"API Documentation","text":"consecutive_overlapping_subspans(span::AlignedSpan, duration::Period,\n                                 hop_duration::Period)\nconsecutive_overlapping_subspans(span::AlignedSpan, n_window_samples::Int, n_hop_samples::Int)\n\nCreate an iterator of AlignedSpan such that each AlignedSpan has n_window_samples (calculated as n_samples(span.sample_rate, duration) if duration::Period is supplied) samples, shifted by n_hop_samples (calculated as n_samples(span.sample_rate, hop_duration) if hop_duration::Period is supplied) samples between consecutive spans.\n\nwarning: Warning\nWhen n_samples(span) is not an integer multiple of n_window_samples, only AlignedSpans with n_window_samples samples will be returned. This is analgous to consecutive_subspans with keep_last=false, which is not the default behavior for consecutive_subspans.\n\nNote: If hop_duration cannot be represented as an integer number of samples, rounding will occur to ensure that all output AlignedSpans will have the same number of samples. When rounding occurs, the output hop_duration will be: Nanosecond(n_samples(samp_rate, hop_duration) / samp_rate * 1e9)\n\n\n\n\n\n","title":"AlignedSpans.consecutive_overlapping_subspans"},{"category":"function","location":"API/#AlignedSpans.n_samples","page":"API Documentation","text":"n_samples(sample_rate, duration::Union{Period, Dates.CompoundPeriod})\n\nReturns the minimal number of samples that can occur in a span of duration.\n\n\n\n\n\nn_samples(aligned::AlignedSpan)\n\nReturns the number of samples present in the span aligned.\n\n\n\n\n\n","title":"AlignedSpans.n_samples"},{"category":"function","location":"API/#AlignedSpans.indices","page":"API Documentation","text":"AlignedSpans.indices(span::AlignedSpan) -> UnitRange{Int64}\n\nReturns the sample indices associated to an AlignedSpan.\n\n\n\n\n\n","title":"AlignedSpans.indices"},{"category":"function","location":"API/#AlignedSpans.start_index_from_time","page":"API Documentation","text":"AlignedSpans.start_index_from_time(sample_rate, span, rounding_mode)\n\nReturns the index of a sample object obtained by rounding the start of span according to rounding_mode.\n\nSee also AlignedSpan(sample_rate, span, mode::SpanRoundingMode) and AlignedSpan(sample_rate, span, mode::ConstantSamplesRoundingMode).\n\n\n\n\n\n","title":"AlignedSpans.start_index_from_time"},{"category":"function","location":"API/#AlignedSpans.stop_index_from_time","page":"API Documentation","text":"AlignedSpans.stop_index_from_time(sample_rate, span, rounding_mode)\n\nReturns the index of a sample object obtained by rounding the stop of span according to rounding_mode.\n\nSee also AlignedSpan(sample_rate, span, mode::SpanRoundingMode).\n\n\n\n\n\n","title":"AlignedSpans.stop_index_from_time"}]
}
